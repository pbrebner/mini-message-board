extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href="/new") Create New Messsage 

  h2 Messages:
  div.messages
    each message in messages
      div.message 
        div 
          strong Message: #{message.text}
        div User: #{message.user} 
        div Added:  #{message.added}
